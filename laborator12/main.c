Copy code
#include <stdio.h>
#include "Produs.h"

int main(int argc, char *argv[]) {
    struct Produs p;
    // utilizeaza argumentele din linia de comanda pentru a initializa structura Produs
    // ex: p.nume_produs = argv[1];
    // ex: p.cantitate = atoi(argv[2]);
    // ex: p.pret_produs = atof(argv[3]);
    return 0;
}